---
$schema: http://json-schema.org/draft-04/schema#
title: EC2 AMI info collection
type: object
properties:
  plugins:
    type: object
    properties:
      ami_info:
        type: object
        properties:
          path: {type: string}
          s3_bucket: {type: string}
          s3_prefix: {type: string}
        additionalProperties: false
